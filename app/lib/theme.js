import { theme as defaultTheme } from 'hypobox'

export const theme = {
  ...defaultTheme,
  color: {
    black: '#333',
    gray: '#555'
  },
  fontFamily: {
    sans: '"Roboto", sans-serif',
    mono: '"Roboto Mono", monospace'
  }
}
